<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Acesso Corporativo - Muscle Way</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #0c0c0f;
        color: #ffffff;
        font-family: "Poppins", sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        text-align: center;
      }

      .main-container {
        width: 100%;
        max-width: 500px;
        padding: 20px;
      }

      h1 {
        color: #00bfff;
        text-transform: uppercase;
        font-size: 1.8rem;
        font-weight: 700;
        letter-spacing: 1px;
        margin-top: 10px;
        margin-bottom: 50px;
        text-shadow: 0 0 10px rgba(0, 191, 255, 0.7),
          0 0 20px rgba(0, 191, 255, 0.5);
      }

      .success-checkmark {
        width: 100px;
        height: 100px;
        margin: 0 auto 20px auto;
      }

      .checkmark-svg {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        display: block;
        stroke-width: 3;
        stroke: #00bfff;
        stroke-miterlimit: 10;
        box-shadow: inset 0px 0px 0px #00bfff;
        animation: fill 0.4s ease-in-out 0.4s forwards,
          scale 0.3s ease-in-out 0.9s both;
      }

      .checkmark-circle {
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        stroke-width: 3;
        stroke-miterlimit: 10;
        stroke: #00bfff;
        fill: none;
        animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
      }

      .checkmark-check {
        transform-origin: 50% 50%;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
      }

      @keyframes stroke {
        100% {
          stroke-dashoffset: 0;
        }
      }

      @keyframes scale {
        0%,
        100% {
          transform: none;
        }
        50% {
          transform: scale3d(1.1, 1.1, 1);
        }
      }

      @keyframes fill {
        100% {
          box-shadow: inset 0px 0px 0px 50px #00bfff22;
        }
      }

      .filter-section {
        background-color: #1a1a1e;
        border: 1px solid #00bfff;
        border-radius: 12px;
        padding: 30px;
        box-shadow: 0 0 25px rgba(0, 191, 255, 0.2);
      }

      .filter-header {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
        margin-bottom: 25px;
        border-bottom: 1px solid #00bfff;
        padding-bottom: 15px;
      }

      .filter-icon {
        color: #00bfff;
      }

      .filter-header h2 {
        margin: 0;
        font-size: 1.2rem;
        font-weight: 600;
        color: #f0f0f0;
      }

      /* Links da lista */
      .filter-options {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 15px;
      }

      .option-link {
        color: #e0e0e0;
        text-decoration: none;
        font-size: 1.1rem;
        transition: color 0.2s, transform 0.2s;
      }

      .option-link:hover {
        color: #00bfff;
        transform: translateX(5px);
      }

      .option-link.active {
        color: #00bfff;
        font-weight: 600;
      }
    </style>
  </head>

  <body>
    <div class="main-container">
      <div class="success-checkmark">
        <svg
          class="checkmark-svg"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 52 52"
        >
          <circle class="checkmark-circle" cx="26" cy="26" r="25" fill="none" />
          <path
            class="checkmark-check"
            fill="none"
            d="M14.1 27.2l7.1 7.2 16.7-16.8"
          />
        </svg>
      </div>

      <h1>Acesso Concedido com Sucesso</h1>

      <div class="filter-section">
        <div class="filter-header">
          <svg
            class="filter-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M3 4H21V6H3V4ZM7 11H17V13H7V11ZM11 18H13V20H11V18Z"
              fill="currentColor"
            />
          </svg>
          <h2>Como você gostaria de filtrar?</h2>
        </div>

        <div class="filter-options">
          <a class="option-link" href="/vendas">Vendas</a>
          <a class="option-link" href="/estoque">Estoque</a>
          <a class="option-link" href="/receber">Receber</a>
          <a class="option-link" href="/pagar">Pagar</a>
          <a class="option-link" href="/dashboard">Bancário</a>
          <a class="option-link" href="/expedicao">Expedição</a>
        </div>
      </div>
    </div>
  </body>
</html>
