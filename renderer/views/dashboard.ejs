<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MUSCLE WAY - Suplementos e Nutrição Esportiva</title>
    <style>
 /* --- RESET E GERAIS --- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background-color: #ffffff;
    color: #1a1a1a;
    line-height: 1.6;
}

/* --- HEADER E NAVEGAÇÃO --- */
.header {
    background-color: #1a1a1a;
    color: white;
    padding: 20px 0;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
}

.logo {
    font-size: 28px;
    font-weight: bold;
    text-shadow: 0 0 20px #3b82f6;
    color: #3b82f6;
}

.nav-items {
    display: flex;
    align-items: center;
    gap: 30px;
    flex-wrap: wrap;
}

.nav-item {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    color: inherit;
}

.nav-item:hover {
    color: #3b82f6;
}

/* --- BARRA DE BUSCA --- */
.search-container {
    position: relative;
    min-width: 300px;
}

.search-input {
    width: 100%;
    padding: 12px 40px 12px 12px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 14px;
}

.search-icon {
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    color: #666;
}

/* --- HERO SECTION (BANNER PRINCIPAL) --- */
.hero {
    position: relative;
    min-height: 80vh;
    /* Imagem de fundo escura para contraste */
    background: linear-gradient(rgba(26, 26, 26, 0.6), rgba(26, 26, 26, 0.6)), url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1920 1080"><rect fill="%23333" width="1920" height="1080"/><text x="960" y="540" fill="%23666" text-anchor="middle" font-size="48">MUSCLE TRAINING</text></svg>');
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.hero-content {
    max-width: 800px;
    color: white;
}

.hero-title {
    font-size: 4rem;
    font-weight: bold;
    margin-bottom: 30px;
    text-shadow: 0 0 30px #3b82f6;
    line-height: 1.1;
}

.hero-subtitle {
    font-size: 1.2rem;
    margin-bottom: 40px;
    opacity: 0.9;
    line-height: 1.6;
}

.cta-button {
    background-color: #1a1a1a;
    color: white;
    border: 2px solid #3b82f6;
    padding: 15px 30px;
    font-size: 18px;
    font-weight: bold;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 0 30px rgba(59, 130, 246, 0.3);
}

.cta-button:hover {
    background-color: #3b82f6;
    transform: translateY(-2px);
    box-shadow: 0 0 40px rgba(59, 130, 246, 0.5);
}

/* --- PRODUTOS (ÁREA CORRIGIDA) --- */
.products {
    padding: 80px 0;
    background-color: #ffffff;
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
}

.product-card {
    background-color: #ffffff; /* Fundo Branco */
    border-radius: 12px;
    padding: 30px;
    text-align: center;
    transition: all 0.3s ease;
    cursor: pointer;
    border: 1px solid #f0f0f0; /* Borda leve */
    box-shadow: 0 4px 15px rgba(0,0,0,0.05); /* Sombra suave */
}

.product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(59, 130, 246, 0.15);
    border-color: #3b82f6;
}

/* Removemos o cinza dessas classes específicas */
.product-card.accessories,
.product-card.kits {
    background-color: #ffffff;
}

/* Mantivemos o estilo escuro apenas para destaques, se quiser */
.product-card.launches,
.product-card.training {
    background-color: #1a1a1a;
    color: white;
    border: none;
}

/* Estilo da área da imagem corrigido para BRANCO */
.product-image {
    width: 100%;
    height: 200px;
    background-color: #ffffff; /* Era cinza, agora é branco */
    border-radius: 8px;
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #666;
    font-size: 14px;
    overflow: hidden;
}

.product-image img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    display: block;
}

/* Textos do Produto */
.product-title {
    font-size: 1.4rem;
    font-weight: 800;
    color: #111;
    margin-bottom: 8px;
    letter-spacing: 0.5px;
}

/* Ajuste de cor para cards escuros */
.product-card.launches .product-title,
.product-card.training .product-title {
    color: #fff;
}

.product-description {
    font-size: 0.9rem;
    color: #555;
    margin-bottom: 6px;
    text-transform: uppercase;
    font-weight: 600;
    opacity: 0.85;
}

.product-card.launches .product-description,
.product-card.training .product-description {
    color: #ccc;
}

.product-price {
    display: block; /* Garante que fique em linha própria */
    font-size: 1.3rem;
    font-weight: 900;
    color: #1d4ed8;
    margin: 10px 0;
}

/* Botão de compra bonito */
.btn {
    display: inline-block;
    padding: 12px 25px;
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
    color: #fff !important;
    text-decoration: none;
    font-weight: bold;
    border-radius: 10px;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.4);
    transition: all 0.25s ease;
    margin-top: 10px;
}

.btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 18px rgba(59, 130, 246, 0.6);
    background: linear-gradient(135deg, #60a5fa, #3b82f6);
}

/* Status do Produto */
.product-status {
    display: block;
    font-size: 0.85rem;
    font-weight: 700;
    margin-bottom: 15px;
}
.product-status.pronta-entrega { color: #16a34a; }
.product-status.indisponivel { color: #dc2626; }

/* --- FOOTER --- */
.footer {
    background-color: #ffffff;
    padding: 60px 0;
    border-top: 1px solid #eee;
    margin-top: 40px;
}

.benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 40px;
    text-align: center;
}

.benefit-item {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.benefit-icon {
    width: 60px;
    height: 60px;
    background-color: #3b82f6;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 24px;
    margin-bottom: 15px;
}

.benefit-title {
    font-size: 1.2rem;
    font-weight: bold;
    margin-bottom: 8px;
    color: #1a1a1a;
}

.benefit-description {
    color: #666;
    font-size: 0.9rem;
}

/* --- MOBILE / RESPONSIVO --- */
@media (max-width: 768px) {
    .header-content {
        flex-direction: column;
        gap: 20px;
    }
    .nav-items {
        flex-direction: column;
        gap: 15px;
        width: 100%;
    }
    .search-container {
        width: 100%;
        min-width: auto;
    }
    .hero-title {
        font-size: 2.5rem;
    }
    .hero-subtitle {
        font-size: 1rem;
    }
    .product-grid {
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 20px;
    }
    /* Ajustes específicos para card mobile */
    .product-card {
        padding: 20px;
    }
}
    </style>
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">MUSCLE WAY</div>
                <div class="nav-items">
                    <a href="/sobrenos" class="nav-item">
                        <span>💊</span>
                        <span>Sobre Nós</span>
                    </a>
                    <div class="search-container">
                        <input type="text" class="search-input" placeholder="O que você procura?">
                        <span class="search-icon">🔍</span>
                    </div>

                    <a href="/dashboard" class="nav-item">
                        <span>🏠</span>
                        <span>Início</span>
                    </a>

                    <a href="/atendimento" class="nav-item">
                        <span>🎧</span>
                        <span>Atendimento</span>
                    </a>
                    </div>
                    <a href="/carrinho" class="nav-item">
                        <span>🛒</span>
                        <span>Carrinho</span>
                    </a>
                    <a href="/sair" class="nav-item">
                        <span>❌</span>
                        <span>Sair</span>
                    </a>
                </div>
            </div>
        </div>
    </header>
    <section class="hero">
        <div class="hero-content">
            <h1 class="hero-title">
                ENERGIA NO POTE,<br>
                RESULTADO NO CORPO.
            </h1>
            <p class="hero-subtitle">
                Uma linha completa de proteínas de alto valor biológico — qualidade das melhores 
                matérias-primas internacionais e sabores incríveis.
            </p>
            <button class="cta-button">CONFIRA AQUI</button>
        </div>
    </section>
<section class="products">
  <div class="container">
    <div class="product-grid">

      <% produtos.forEach(produto => { %>
        <div class="product-card">
          
          <% if (produto.categoria === 'Kit Pronto') { %>
            <span class="badge">Destaque</span>
          <% } %>

          <div class="product-image">
            <!-- Aqui puxamos a imagem com o nome igual ao produto ou cod_produto -->
            <img src="/images/<%= produto.cod_produto %>.png" alt="<%= produto.nome_produto %>">
          </div>

          <h3 class="product-title"><%= produto.nome_produto %></h3>
          <p class="product-description"><%= produto.subcategoria %></p>
          <p class="product-price">
            R$ <%= Number(produto.preco_real || 0).toFixed(2) %>
          </p>
          <p class="product-status"><%= produto.disponibilidade %></p>
          <p class="product-status <%= produto.disponibilidade.replace(/\s+/g, '-').toLowerCase() %>">
            <%= produto.disponibilidade %>
          </p>
 
          <a href="/produto/<%= produto.cod_produto %>" class="btn">Ver produto</a>
        </div>
      <% }) %>

    </div>
  </div>
</section>

